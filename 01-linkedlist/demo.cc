// Data Structures and Algorithm Analysis in C (Second Edition) 
// http://users.cs.fiu.edu/~weiss/dsaa_c2e/list.c
#include <stdio.h>
#include <stdlib.h>
typedef struct node *node_ptr;
typedef double element_type;
struct node{
	element_type element;
	node_ptr next;
};

int main(){

		return 0;
}